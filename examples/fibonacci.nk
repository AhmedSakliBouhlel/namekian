// Fibonacci in Namekian

int fibonacci(int n) {
  if (n <= 1) {
    return n;
  }
  return fibonacci(n - 1) + fibonacci(n - 2);
}

// Print first 10 fibonacci numbers
for (int i = 0; i < 10; i = i + 1) {
  print(fibonacci(i));
}
